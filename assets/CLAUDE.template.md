<!--
  Generated by the claude-for-idiots skill.
  Replace every {{PLACEHOLDER}} during onboarding, then write to <project>/CLAUDE.md.
  If a CLAUDE.md already exists, MERGE this content instead of overwriting it.
-->

# Project guide (managed by claude-for-idiots)

## How to talk to me
- **Language:** {{LANGUAGE}} — always respond in this language.
- **Experience level:** {{EXPERIENCE_LEVEL}}
- **Technical terms:** {{TERM_MODE}}
  - `none` → never use jargon; always paraphrase.
  - `explain` → use the term + a short explanation; ramp up with my level; keep the glossary at `.claude-for-idiots/glossary.json` updated.
  - `raw` → use terms normally, no explanations.

## At the start of work
- If term mode is `explain`: read `.claude-for-idiots/glossary.json` first and
  use it to avoid re-explaining terms; update it whenever you explain a new term
  or go a level deeper on an existing one. (This file is NOT auto-loaded — you
  must read it.)
- Full machine-readable config (also read by the enforcement hooks) lives in
  `.claude-for-idiots/config.json`.

## Stack
{{STACK_SUMMARY}}

## Architecture — frozen for this project
{{ARCHITECTURE_NAME}}

```
{{FOLDER_LAYOUT}}
```

Layer responsibilities:
{{LAYER_RESPONSIBILITIES}}

New code files MUST stay inside this structure (Rule 5). To change the
architecture, update this section AND `.claude-for-idiots/config.json` first.

## Rules (always apply)

1. **Never hand-edit migration files.** Change the models/schema and run:
   `{{MIGRATION_COMMAND}}`. Never open files under `{{MIGRATION_PATHS}}` to edit them.
2. **Always have tests** (unit + integration, front + back). Run only the
   relevant tests during a feature; **ask before running the full suite.**
   Tests enabled: {{TESTS_ENABLED}}.
3. **Commit per feature** with a clear message ({{VCS_STATUS}}).
4. **Sanity check after each feature:** lint + type-check → the feature's tests
   → smoke test (boot the app and verify it responds).
5. **Keep new files inside the architecture above.**
6. **Secrets never reach the remote.** Keys/tokens/passwords live in a
   gitignored `.env` (with a committed `.env.example`). Local Git is free; any
   publishing action (push, create remote repo, make public, open PR) requires
   my explicit confirmation AND passes a secret scan first.

## Commands
- Relevant tests (per feature): `{{FEATURE_TEST_CMD}}`
- Full suite (ask first): `{{FULL_SUITE_CMD}}`
- Lint / type-check: `{{LINT_CMD}}`
- Run app (smoke test): `{{RUN_CMD}}`

> Config: `.claude-for-idiots/config.json`. Rules 1, 5, 6 are also enforced by
> hooks in `.claude/hooks/` — they will block violations, not just warn.
